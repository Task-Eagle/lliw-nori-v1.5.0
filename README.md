# lliw-nori-v1.5.0
Added a "Delete All History" button, and added a feature to search on eBay/Google using "researchItem() {" function. Item Name now included as an input and also saved in the newDeal object. To be able to research the item in two new browser tabs, we used encodeURIComponent so the browser can understand.
